# Amarktai App Builder Test Notes

Use `ADMIN_EMAIL` and `ADMIN_PASSWORD` from your local `.env` for manual login tests.

Suggested public import test:

```text
https://github.com/octocat/Hello-World
```
