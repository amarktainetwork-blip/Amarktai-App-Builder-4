# nginx/builder.amarktai.com.conf
# Nginx reverse-proxy config for the production deployment at builder.amarktai.com.
#
# Prerequisites:
#   sudo apt-get install -y certbot python3-certbot-nginx
#   sudo certbot --nginx -d builder.amarktai.com
#
# After certbot runs, it will add the SSL/certificate block automatically.
# Reload nginx after any change:
#   sudo nginx -t && sudo systemctl reload nginx

server {
    listen 80;
    server_name builder.amarktai.com;

    # Redirect HTTP → HTTPS (certbot will manage this block)
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name builder.amarktai.com;

    # certbot manages these certificate paths
    ssl_certificate     /etc/letsencrypt/live/builder.amarktai.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/builder.amarktai.com/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    # Security headers
    add_header X-Frame-Options SAMEORIGIN;
    add_header X-Content-Type-Options nosniff;
    add_header Referrer-Policy strict-origin-when-cross-origin;

    # ── API / Backend ────────────────────────────────────────────────────────
    # Production backend runs on port 8001 (see docker-compose.yml BACKEND_PORT)
    location /api/ {
        proxy_pass         http://127.0.0.1:8001;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }

    # ── WebSocket ─────────────────────────────────────────────────────────────
    location /api/ws/ {
        proxy_pass         http://127.0.0.1:8001;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade    $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host       $host;
        proxy_read_timeout 600s;
    }

    # ── Frontend ─────────────────────────────────────────────────────────────
    # Production frontend runs on port 8080 (see docker-compose.yml FRONTEND_PORT)
    location / {
        proxy_pass         http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }
}
